1) https://www.youtube.com/watch?v=YD1gxA_m--I&t=359s (Spring Pet Clinic (Sample application provided by Spring) | Spring Boot | Thyme Leaf)
https://github.com/spring-projects/spring-petclinic

https://spring-petclinic.github.io/
https://github.com/spring-petclinic/

REFERENCES to archived project:
https://github.com/spring-attic/spring-mvc-showcase

REFERENCES to actively maintained sample projects:
https://spring.io/guides


2) Spring Security ( Laur Spilca )
https://www.youtube.com/watch?v=dFvbHZ8CuKM&list=PLEocw3gLFc8X_a8hGWGaBnSkPFJmbb8QP&index=2  ( Spring Security Fundamentals - Lesson 2 - Managing users )


https://stackoverflow.com/questions/11361331/how-to-download-sources-for-a-jar-with-maven

  <plugin>
    <groupId>org.apache.maven.plugins</groupId>
    <artifactId>maven-dependency-plugin</artifactId>
    <version>3.1.1</version>
    <executions>
      <execution>
        <id>download-sources</id>
        <goals>
          <goal>sources</goal>
        </goals>
        <configuration>
        </configuration>
      </execution>
    </executions>
  </plugin>
  
3) How to create your first Spring Application (without Spring Boot)

4) convert the spring application from folder 3 above to a a spring-boot application.

5) example of a Spring Boot application that demonstrates the use of the @Primary annotation. This example shows how Spring resolves bean conflicts by preferring the @Primary bean unless explicitly overridden using @Qualifier.

6) Spring Boot - @Autowired, @Qualifier, @Primary, @Required

7) Simple spring-boot application with dependecy, springdoc-openapi-starter-webmvc-ui

8) A sample Spring Boot application that demonstrates how to implement springdoc-openapi-starter-webmvc-ui for generating and displaying OpenAPI documentation using Swagger UI.

9) using @Value to inject values from application.properties

10) @ConfigurationProperties

11) Spring boot profiles

12) By default, Spring Boot uses Logback as the logging implementation with SLF4J

13) @RequestParam , @PathVariable

14) @RequestParam , @PathVariable

15) one more example to demonstrate @RequestParam , @PathVariable with model, service and repository layers added.

16) Same example from 15) above , just added service interface , repository interface

17) @ControllerAdvice, in-memory H2 database

18) Dan Vega: Spring Boot API Integration: Debug Common Issues

19) This Spring Boot client demonstrates how to call a public API using RestTemplate or WebClient.
V1: using RestTemplate
V2: using WebClient (Reactive Approach)

20) Parse an Excel document and convert it into JSON.

21) clinicalsapi
This spring boot project works but i need to make some more enhancements.
Udemy course: AI with GitHub Copilot for Java & Spring Boot Developers

22) Upload File to AWS S3 Bucket
V1: used AWS SDK V1
V2: used AWS SDK V2
V3: reads the bucket name from application.properties rather than hardcoding it.


 
